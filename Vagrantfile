# -*- mode: ruby -*-
# vi: set ft=ruby :

# This Vagrantfile assumes a box built the matching packer template from packer/ubuntu/$VERSION.json
# See packer/ubuntu/README for more information.

VAGRANTFILE_API_VERSION = "2"

Vagrant.configure(VAGRANTFILE_API_VERSION) do |config|
  config.vm.box = "collectd-librato-ubuntu-12.04.4-0"
end
